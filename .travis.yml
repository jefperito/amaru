language: python

python:
  - "3.3"
  - "3.4"
  
install:
  - sudo add-apt-repository --yes ppa:ubuntu-sdk-team/ppa
  - sudo apt-get update
  - pip install python-qt5
  
before_script:
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start
  
script:
  - python -m unittest -v
